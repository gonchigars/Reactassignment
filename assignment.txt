 import React from 'react';
import { Container, Box, Button } from '@mui/material';

const VerticalButtons = () => {
  return (
    <Container>
      <Box sx={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 80 }}>
      <Button variant="contained"Button sx={{ mt: 2, mb: 2 }}>Button 1</Button>
      <Button variant="contained"Button sx={{ mt: 2, mb: 2 }}>Button 2</Button>
      
      </Box>
       
    </Container>
  );
};

export default VerticalButtons;
