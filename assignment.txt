*ASSIGNMENT1*

import React from 'react';
import { Button, Box } from '@mui/material';

const VerticalButton = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
         
      }}
    >
      <Button
        variant="contained"
        sx={{ mt: 2, mb: 2 }} 
      >
        Button 1
      </Button>OK
      <Button
        variant="contained"
        sx={{ mt: 2, mb: 2 }}
      >
        Button 2
      </Button>CLEAR
      <Button
        variant="contained"
        sx={{ mt: 2, mb: 2 }}
      >
        Button 3
      </Button>SUBMIT
    </Box>
  );
};

export default VerticalButton;





*ASSIGNMENT2*
import React from 'react';
import { Button, Box, } from '@mui/material';

const HorizontalButtonLayout = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        justifyContent: 'space-between', 
        alignItems: 'center', 
        mx: 4, 
        ml:20
      }}
    >
      <Button variant="contained">
        Button 1
      </Button>
      <Button variant="contained">
        Button 2
      </Button>
      <Button variant="contained">
        Button 3
      </Button>
    </Box>
  );
};

export default HorizontalButtonLayout;





*ASSIGNMENT3*
import React from 'react';
import { AppBar, Toolbar, Typography, Button, Box } from '@mui/material';

const AppBarWithButtons = () => {
  return (
    <AppBar position="static">
      <Toolbar>
        <Typography
          variant="h6"
          component="div"
          sx={{ flexGrow: 1,ml:30 }} 
        >
          My App
        </Typography>

        <Box>
          <Button color="inherit">Login</Button>
          <Button color="inherit">Signup</Button>
        </Box>
      </Toolbar>
    </AppBar>
  );
};

export default AppBarWithButtons;





*ASSIGNMENT4*
import React from 'react';
import { Drawer, List, ListItem, ListItemButton, ListItemText, Box, CssBaseline } from '@mui/material';

const Sidebar = () => {
  
  const drawerWidth = 150;

  
  const navigationOptions = ['Home', 'Profile', 'Settings'];

  return (
    <Box sx={{ display: 'flex' }}>
      <CssBaseline />
      <Drawer
        variant="permanent"
        sx={{
          width: drawerWidth,
          flexShrink: 0,
          [`& .MuiDrawer-paper`]: { width: drawerWidth, boxSizing: 'border-box' },
        }}
      >
        <List>
          {navigationOptions.map((text, index) => (
            <ListItem key={text} disablePadding>
              <ListItemButton>
                <ListItemText primary={text} />
              </ListItemButton>
            </ListItem>
          ))}
        </List>
      </Drawer>
      <Box
        component="main"
        sx={{ flexGrow: 1, p: 3 }}
      >
        {/* Main content goes here */}
        <h1>Welcome to My App</h1>
      </Box>
    </Box>
  );
};

export default Sidebar;





*ASSIGNMENT5*
import React from 'react';
import { Box, Button } from '@mui/material';

const CenteredContent = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        flexDirection: 'column',       
        justifyContent: 'center',      
        alignItems: 'center',          
        height: '100vh',               
      }}
    >
      <Box sx={{ gap: 2, display: 'flex', flexDirection: 'column' }}>
        <Button variant="contained" sx={{ my: 2 }}>
          Centered Button
        </Button>
      </Box>
    </Box>
  );
};

export default CenteredContent;




*ASSIGNMENT6*
import React from 'react';
import { Grid, Button, Box } from '@mui/material';

const ResponsiveGrid = () => {
  
  const buttons = ['Button 1', 'Button 2', 'Button 3', 'Button 4', 'Button 5', 'Button 6'];

  return (
    <Box sx={{ flexGrow: 1, p: 2,ml:30 }}>
      <Grid container spacing={2}>
        {buttons.map((label, index) => (
          <Grid 
            item 
            key={index} 
            xs={12}  
            sm={6}   
            md={4}   
          >
            <Button 
              variant="contained" 
              fullWidth 
              sx={{ py: 2 }} 
            >
              {label}
            </Button>
          </Grid>
        ))}
      </Grid>
    </Box>
  );
};

export default ResponsiveGrid;



*ASSIGNMENT7*
import React from 'react';
import { Box, Button, Typography } from '@mui/material';

const FixedButton = () => {
  return (
    <Box sx={{ position: 'relative', minHeight: '200vh' }}>
      <Typography variant="h4" align="center" sx={{ p: 3 }}>
        
      </Typography>
      <Button
        variant="contained"
        color="primary"
        sx={{
          position: 'fixed',
          bottom: 16,
          right: 16,
          zIndex: 1000,  
        }}ac
      >
        Fixed Button
      </Button>
    </Box>
  );
};

export default FixedButton;



*ASSIGNMENT8*
import React from 'react';
import { Card, CardContent, CardActions, Button, Typography } from '@mui/material';

const CardWithButtons = () => {
  return (
    <Card sx={{ maxWidth: 345, margin: 'auto', mt: 5 }}>
      <CardContent>
        <Typography variant="h5" component="div" gutterBottom>
          Card Title
        </Typography>
        <Typography variant="body2" color="text.secondary">
          This is an example of a card with multiple action buttons at the bottom.
          RAMESHWAR.........
        </Typography>
      </CardContent>
      <CardActions>
        <Button size="small" color="primary">
          Accept
        </Button>
        <Button size="small" color="secondary">
          Decline
        </Button>
      </CardActions>
    </Card>
  );
};

export default CardWithButtons;



*ASSIGNMENT9*
import React, { useState } from 'react';
import {
  Dialog,
  DialogActions,
  DialogContent,
  DialogContentText,
  DialogTitle,
  Button,
} from '@mui/material';

const DialogWithConfirmation = () => {
  const [open, setOpen] = useState(false);

  const handleClickOpen = () => {
    setOpen(true);
  };

  const handleClose = () => {
    setOpen(false);
  };

  const handleConfirm = () => {
    
    console.log('Confirmed!');
    setOpen(false);
  };

  return (
    <div>
      <Button sx={{ml:35}}variant="contained" color="primary" onClick={handleClickOpen}>
        Open Dialog
      </Button>

      <Dialog open={open} onClose={handleClose}>
        <DialogTitle>{"Confirmation Dialog"}</DialogTitle>
        <DialogContent>
          <DialogContentText>
            Are you sure..........?
          </DialogContentText>
        </DialogContent>
        <DialogActions>
          <Button onClick={handleClose} color="primary">
            Cancel
          </Button>
          <Button onClick={handleConfirm} color="primary" autoFocus>
            Confirm
          </Button>
        </DialogActions>
      </Dialog>
    </div>
  );
};

export default DialogWithConfirmation;



*ASSIGNMENT10*
import React from 'react';
import { 
  Container, Grid, Paper, Typography, Fab, Box, 
  AppBar, Toolbar, Button 
} from '@mui/material';
import AddIcon from '@mui/icons-material/Add';

function App10() {
  return (
    <Box sx={{ display: 'flex', flexDirection: 'column', minHeight: '100vh' }}>
      <AppBar position="static">
        <Toolbar>
          <Typography variant="h6" sx={{ flexGrow: 1,ml:25 }}>My App</Typography>
          <Button color="inherit">Login</Button>
        </Toolbar>
      </AppBar>

      <Container sx={{ flexGrow: 1, position: 'relative', my: 2 }}>
        <Grid container spacing={2}>
          <Grid item>
            <Paper sx={{ p: 2 ,ml:8}}>
              <Typography>Left Column</Typography>
            </Paper>
          </Grid>
          <Grid item>
            <Paper sx={{ p: 2 }}>
              <Typography>Right Column</Typography>
            </Paper>
          </Grid>
        </Grid>
        <Fab color="primary" sx={{ position: 'absolute', bottom: 16, right: 16 }}>
          <AddIcon />
        </Fab>
      </Container>

      <Box component="footer" sx={{ bgcolor: 'background.paper', p: 2,position:'relative',bottom:0 }}>
        <Typography variant="body2" color="text.secondary" align="center">
          © 2024 My App. All rights reserved.
        </Typography>
      </Box>
    </Box>
  );
}

export default App10;
